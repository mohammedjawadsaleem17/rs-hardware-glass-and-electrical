services:
  - type: web
    name: rs-hardware-backend
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    # Remove buildCommand - not needed with multi-stage build
    envVars:
      - key: PORT
        value: 8080